# mobileTestGame

Empty **Godot 4** project — open this folder in the editor (`project.godot`) and run with **F5**.

Android export presets live in `export_presets.cfg` if you configure the SDK later.
